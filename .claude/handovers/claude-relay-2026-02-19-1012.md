# Handover: claude-relay - 2026-02-19 10:12

## Current Task
Planning migration of claude-relay from Node.js to Elixir/Phoenix.

## Status
- [x] Codebase exploration complete (full architecture mapped)
- [x] Migration plan written and approved
- [ ] Implementation not started — new repo needs to be created
- Current branch: `sandbox`
- Last commit: `8df0b42 - tools versions added`

## Immediate Next Steps
1. Create new Elixir/Phoenix project: `mix phx.new claude_relay --no-mailer --no-dashboard --no-gettext`
2. Start Phase 1 from plan: scaffold + Claude API streaming MVP
3. Implement `ClaudeRelay.Anthropic.Client` — Req-based SSE streaming to Messages API

## Key Decisions Made
- **Direct HTTP API** (not Agent SDK): Pure Elixir, no Node.js dependency. Call Anthropic Messages API directly with tool use.
- **Phoenix LiveView**: Server-rendered with JS hooks for streaming text, xterm.js, mermaid, syntax highlighting.
- **SQLite now, Postgres later**: Ecto + ecto_sqlite3 for local tool, easy swap later.
- **Core chat first**: Phase 1-2 (chat + tools), then sessions (Phase 3), terminal (Phase 4), polish (Phase 5).
- **No daemon needed**: OTP supervision replaces the Node.js daemon/IPC architecture entirely.

## Blockers / Open Questions
- None currently — plan approved, ready to implement.

## Context the Next Session Needs
- **Migration plan**: `/home/johan/.claude/plans/compressed-wobbling-hickey.md` — full 5-phase plan with file structures, schemas, architecture
- **Codeindex**: `.codeindex/output/overview.md` has project structure summary (390 symbols, 27 files)
- **Reference code**: `lib/sdk-bridge.js` (579 lines) is the core Claude integration to rewrite; `lib/project.js` (819 lines) is the main server orchestration
- **User's stack**: Elixir/Phoenix preferred, on Claude Max x5 (Opus 4.6)
- **This repo** stays as reference — new Elixir project goes in a separate repo
- **The app was tested locally**: `node bin/cli.js -p 5000` works, npm installed, but not installed globally

## Running Processes
- None — claude-relay daemon was shut down during session
